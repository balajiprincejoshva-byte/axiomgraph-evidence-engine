# AxiomGraph Backend — Environment Variables
# Set these in Railway dashboard → Variables tab

# Frontend Vercel URL (for CORS) — set after deploying frontend
ALLOWED_ORIGINS=https://axiomgraph.vercel.app,http://localhost:3000
